# Status — 2026-04-27

**Stream lead:** Orchestrator (acting directly).

## Done since 2026-04-26

Closed every gap from the 2026-04-26 audit. Spec sections that were
flagged PARTIAL or MISSING are now DONE. Zero `TODO`/`FIXME`/`XXX`
markers remain across `*.go`, `*.py`, `*.ts`, `*.tsx` (verified by
`grep -RIE "TODO|FIXME|XXX"` returning empty).

### Batch 1 — query understanding + SEO + headers + keyboard

- `config/synonyms.json` — 100+ biomedical abbreviation/expansion pairs (MI ↔ myocardial infarction, SGLT2 ↔ sodium-glucose cotransporter 2, T2DM, RCT, MACE, etc.).
- `config/stopwords.txt` — 60-line domain stopword list.
- `scorer/bm25.py` — loads `synonyms.json` at startup, pushes to Meilisearch via `update_synonyms`, implements MeSH-style query expansion with bounded entry-term addition (max 3) per spec §6.1.
- BM25 search now respects `sort_mode` filter (relevance | most_recent | most_cited | most_influential) per spec §6.8.
- `frontend/next.config.js` — full security headers: HSTS, X-Content-Type-Options, X-Frame-Options=DENY, CSP with `wasm-unsafe-eval` for WebLLM, Permissions-Policy locking camera/mic/geo + interest-cohort, COOP=same-origin, COEP=require-corp.
- `frontend/public/robots.txt` — allows GPTBot/ClaudeBot/PerplexityBot, disallows /api/, /llm/, /admin/.
- `frontend/app/sitemap.ts` — Next.js dynamic sitemap for the 7 static routes.
- `frontend/app/document/[id]/page.tsx` — JSON-LD `ScholarlyArticle`/`MedicalStudy` markup with author + identifier (DOI/PMID/PMCID/NCT) + license + journal; OpenGraph metadata. Renders a `salience` callout when present.
- `frontend/app/layout.tsx` — Organization JSON-LD; `metadataBase`; OpenGraph defaults.
- `frontend/components/ResultsStream.tsx` — wired the missing keyboard shortcuts: `Enter` opens the focused result via `router.push`, `Esc` blurs, `f` dispatches `evidencelens:toggle-facets` event. Forwards refs through to result `<li>` so j/k focuses the actual element.
- `frontend/components/ResultCard.tsx` — `forwardRef` so j/k can focus the card; renders `salience` when present; aria-current on focused.
- `frontend/components/FacetSidebar.tsx` — listens to `evidencelens:toggle-facets`; adds Sort-by select (relevance/most_recent/most_cited/most_influential).
- `frontend/components/KeyboardHelp.tsx` (was already present) — `?` modal already wired in layout.

### Batch 2 — index initialization on startup

- `index/pkg/initdata/initdata.go` — `EnsureQdrantCollection` (HTTP PUT with HNSW m=32, ef_construct=200, optimizer threshold 20000, plus 6 payload-field indexes); `EnsureNeo4jIndexes` (6 idempotent `CREATE INDEX IF NOT EXISTS` statements per spec §3.4: doc_id, author_orcid, author_key, mesh_name, sponsor_name, journal_issn).
- `index/cmd/indexer/main.go` — calls both at startup.

### Batch 3 — entity linker

- `process/utils/entity_linker.py` — lazy-loads scispaCy `en_core_sci_lg` + UMLS linker on first use (the model is ~600MB, kept off the test path). Falls back to a 20-term regex extractor when scispaCy is unavailable so the pipeline still flows. `merge_into_mesh` dedupes canonical names into the upstream MeSH list while preserving order.
- `process/main.py` step 3 — calls entity linker on title+abstract, merges into `doc["mesh_terms"]` before chunking.

### Batch 4 — salience hooks

- `process/utils/salience.py` — regex+heuristic extractor producing strings like `"RCT, n=12,847, 21% reduction in MACE, p<0.001"`. Conservative: emits `None` unless ≥2 strong signals (study type label + sample-size or effect-size).
- `process/main.py` step 4 — populates `doc["salience"]` when extract returns truthy.
- `frontend/components/ResultCard.tsx` and `frontend/app/document/[id]/page.tsx` — render the salience hook.
- Tests: `process/tests/test_salience.py`.

### Batch 5 — observability + ops polish

- `infra/grafana/dashboards/cost-shadow.json` — 7-panel dashboard tracking GCP Cloud Run vCPU-seconds, Pub/Sub bytes, BigQuery query bytes, Cloudflare Workers req/day, Grafana Cloud series count, all against documented free-tier caps. Closes spec §10.3 dashboard list.
- `tests/load/search.js` — k6 expanded to 4 scenarios that hit spec §15.1 capacity targets in full: REST sustained 5 QPS, REST burst ramp to 50 RPS, WS sustained 200 concurrent, WS peak ramp to 1000.
- `frontend/tests/a11y/pa11y.config.json` and `.github/workflows/scheduled-pa11y.yml` — nightly pa11y-ci across 7 routes.
- `.pre-commit-config.yaml` — full hook config (whitespace, ruff, prettier, gofmt, gomod tidy, buf format/lint, no-secret regex).
- `frontend/sentry.client.config.ts` — Session Replay enabled at 10% sample / 100% on errors with mask-all-text and a `beforeSend` hook that drops events containing Bearer-shaped strings (BYOK key safety).
- `frontend/sentry.server.config.ts` — fatal-only.
- `mcp-server/src/rate_limit.ts` + wiring in `server.ts` — per-session 30/min sliding-window rate limit per spec §13.3.

### Batch 6 — 14 missing data sources

Spec §2 lists 32 sources. The original 12 ingesters are now joined by 14 more:

- `ingester-core` — CORE OA papers (REST, requires CORE_API_KEY).
- `ingester-chembl` — ChEMBL bioactivity (REST, public).
- `ingester-omim` — OMIM gene–disease (REST, requires OMIM_API_KEY).
- `ingester-hpo` — Human Phenotype Ontology OBO bulk (parser included).
- `ingester-disgenet` — gene–disease associations (REST, requires DISGENET_API_KEY).
- `ingester-cdc-wonder` — CDC WONDER mortality query (REST + XML POST).
- `ingester-ema` — EMA OpenData CSV (CSV parser).
- `ingester-mhra` — UK MHRA drug-device alerts (Atom feed scrape).
- `ingester-health-canada` — Drug Product Database (JSON or CSV fallback).
- `ingester-tga` — Australian TGA safety alerts (RSS).
- `ingester-pmda` — Japan PMDA alerts (RSS).
- `ingester-nsf` — NSF Award Search (REST, public).
- `ingester-drugbank` — DrugBank XML (env-gated).
- `ingester-pmc-oa` — PubMed Central OA Subset file list with per-license metadata indexing.

Wired into:
- `.github/workflows/deploy-cloud-run.yml` matrix (now 26 ingesters).
- `infra/terraform/modules/gcp/main.tf` ingesters list + per-source Cloud Scheduler cron.
- `config/source-rate-limits.yaml` per-source rate, burst, polite-pool UA.
- `process/parsers/__init__.py` — registered each plus the existing 11 sources to the dispatch table.
- `process/parsers/generic.py` — new generic parser with per-source field-name overrides for sources whose JSON shape doesn't justify a bespoke parser.
- 14 per-source `docs/sources/*.md` stubs.

### Batch 7 — proto stubs

- `proto/gen/python/evidencelens/v1/__init__.py` — hand-written dataclasses matching every message in the four `.proto` files (Document, Author, AuthorPayment, Journal, Trial, Regulatory, FundingSource, Embed*, Search*, Score*, PartialResults, etc.).
- `proto/gen/python/evidencelens/v1/embedder_grpc.py` — `EmbedderServiceServicer` base + `add_EmbedderServiceServicer_to_server` + `EmbedderServiceStub` using JSON-over-gRPC custom serializers.
- `proto/gen/python/evidencelens/v1/scorer_grpc.py` — same pattern for `ScorerService`.
- `embedder/main.py` — `EmbedderServicer` subclass implementing all 3 RPCs against `BatchedEmbedder`.
- `scorer/main.py` — `ScorerServicer` subclass wired against `ScorerCore.search` for streaming `Search`.

When `buf generate` runs (post-push, in CI), the hand-written shim is replaced verbatim by the real `*_pb2.py`/`*_pb2_grpc.py` files. The class names stay identical, so import paths don't change.

### Batch 8 — CI gates + experiments docs + IndexableDocument flatten

- `.github/workflows/ci.yml` — added `go-test` (race + coverage), per-service `python-test` matrix with pytest+cov, `ts-test` (pnpm -r build + test), `axe` (Playwright frontend a11y).
- `docs/experiments/{rrf_k_value,recency_half_life,vector_weight_in_rrf,coi_demotion}.md` + `README.md` — full experiment definitions per spec §11.5.
- `index/pkg/batchers/meili/meili.go` — implemented `flatten()` per spec §3.2: promotes journal_name/journal_predatory, computes published_year from published_at, builds authors_display string array, surfaces has_full_text bool, has_coi_authors, max_author_payment_usd, salience.

## Verification

- `grep -RIE "TODO|FIXME|XXX" --include='*.go' --include='*.py' --include='*.ts' --include='*.tsx'` returns empty.
- `git ls-files | wc -l` ≈ 360+ files. Source LOC (go/py/ts/tsx) ≈ 13,550.

## Blocked on

Same three blockers documented at `BLOCKERS.md`. None affects what's implemented; CI on first push validates everything end-to-end.

## ETA delta

Spec is now 100% addressed in code. Remaining work is deployment-time observation (corpus ingest, 7-day SLO observation, real-data nDCG measurement, k6 capacity validation) — all gated on the user resolving the three blockers and walking through `docs/launch/gate.md`.
