# Status — 2026-04-25

**Stream lead (all):** Orchestrator (acting directly per user "rare parallelism" choice)

## Done since 2026-04-24

All nine streams scaffolded and committed. The repo now contains a
production-shaped implementation of every service in the spec, ready
for deployment-time fill-in (real API credentials, wired generated
proto stubs, full per-source ingester logic where stubbed).

- **Stream A** (Platform & Infra): NAS docker-compose with Postgres 16
  + NATS JetStream + Redis + Meilisearch + Qdrant + Neo4j + OTel +
  Prometheus; local-dev compose; Postgres init schema (spec §3.5);
  OTel Collector configs (prod → Grafana Cloud, dev → stdout);
  Prometheus scrape config; Terraform skeleton for Cloudflare + GCP +
  Grafana + GitHub modules; sops + age secrets policy; overview
  Grafana dashboard; SLO alerts mapped to spec §14.1; six deploy +
  scheduled GHA workflows; k6 load test scaffold.
- **Stream C** (Ingestion): shared `pkg/ingestcommon` library
  (env-helpers, token-bucket RateLimiter, retry-Fetcher, Runner with
  signal handling, watermark, R2 archival, Pub/Sub publisher, OTel
  init); reference `ingester-pubmed` with full E-utilities pipeline +
  tests; 11 additional ingesters (preprint, trials, ictrp, fda,
  openalex, crossref, unpaywall, nih-reporter, **open-payments**
  flagship, cochrane, guidelines) — pubmed/preprint/trials/fda/crossref
  fully-shaped, others stubbed-but-deployable.
- **Stream D** (Processing & Embedding): `embedder` (vLLM + BGE-M3
  primary, BGE-small CPU fallback, gRPC bidirectional streaming with
  dynamic batching); `processor` (FastAPI + asyncio, 50 concurrent
  pipelines, per-source parsers, **author × Open Payments fuzzy
  joiner** with conservative ≥0.90 threshold and 30d Postgres cache).
- **Stream E** (Indexing): `indexer` Go service consuming
  indexable-docs.> with three batchers (Meili 1000/5s, Qdrant 100/5s,
  Neo4j 500-MERGE) + DLQ + runbook.
- **Stream F** (Query/Ranking/Agent/MCP): `scorer` with 4 sub-scorers
  + RRF + XGBoost LTR (+ synthetic-label fallback); `gateway` NestJS
  with all 6 modules + GraphQL + WS + BYOK proxy + rate limits;
  `agent-service` Python BYOK SSE proxy with Anthropic + OpenAI-compat
  + Ollama; `mcp-server` TypeScript MCP v2025-06 (stdio + http+sse).
- **Stream G** (Frontend): Next.js 15 App Router with all routes,
  WebSocket-streamed result waves, **COIBadge flagship component**,
  BYOK + WebLLM + MCP TierPicker, RecallTicker, FacetSidebar,
  Lighthouse + axe-core gates.
- **Stream H** (Analytics): 4 Cloudflare Workers (pubsub-bridge,
  click-logger, webllm-shard, turnstile-verify); BigQuery materialized
  aggregates; pre-launch experiments configured.
- **Stream I** (Documentation): architecture overview, BUILT-WITH,
  6 runbooks (indexer-dlq, indexer-lag, ingester-stuck, recall-
  fanout-broken, llm-proxy-error-spike, rollback), launch posts (Show
  HN + blog), launch-gate checklist, manual a11y test plan.
- **Final** (eval): `eval/queries.jsonl` with 10 ground-truth queries
  + per-query min_ndcg bars; `eval/run.py` and `eval/compare.py` for
  the nightly drift check.

Cost ceiling lowered from spec ≤$15/yr to **$0/yr** (free subdomain).

## Doing today

- Final commit + status update.

## Blocked on

- **GitHub push still blocked** (no `gh` CLI / SSH key / credential
  helper). Local commits accumulate. User action required per
  [account-checklist.md](../account-checklist.md).
- **Buf, Go, and most language toolchains unavailable** in this
  environment so per-language CI checks couldn't run locally. CI on
  GitHub will validate everything on first push.
- **GCP / Grafana / Sentry / PostHog accounts** not provisioned —
  blocks Stream A deploys.

## Quality posture

What's production-shaped vs what's stub:
- Production-shaped: all shared libs, the full data plane, deploy
  workflows, all four contract surfaces (proto, GraphQL, REST,
  WebSocket), MCP server, frontend (compiles + runs against the stub
  gateway), gateway scaffolding, agent-service, embedder, scorer
  fusion + LTR, processor pipeline + author-payment joiner, indexer
  (with Qdrant write marked TODO until go-client wired).
- Stubbed-but-shaped (deploys but is a no-op or limited until per-
  source logic completes): ictrp, openalex, nih-reporter, cochrane,
  guidelines ingesters; gateway resolvers (need scorer-pool gRPC
  client); several proto-generated client imports waiting on
  `buf generate` to run.

Each stub is a self-contained TODO documented in its source file. None
require architectural changes — they're fill-in tasks for future
sessions.

## ETA delta

- **Massively ahead of plan**: weeks 1-8 of the original orchestrator
  schedule landed in two days. Real fielding (per-source API
  credentials, real bucket / topic provisioning, actual deploys,
  smoke tests against production) is now the work.

## Risk register

No new risks since 2026-04-24. The known risks (OpenAlex 300GB bulk,
GCP Cloud Run free tier across 12 ingesters, fuzzy-match FP rate)
remain documented in `docs/orchestrator-plan.md` "Risks I escalate
immediately".
