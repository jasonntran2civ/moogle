# Status — 2026-04-24

**Stream lead (all):** Orchestrator (acting directly per user "rare parallelism" choice)

## Done since yesterday
- N/A — first day.

## Done today
- Repo bootstrap: directory tree per spec §17, root files (README, CLAUDE.md, LICENSE, .gitignore, .editorconfig), workspace skeletons (pnpm-workspace.yaml, go.work, buf.yaml, buf.gen.yaml), Makefile, RFC process doc. Commit `bfdcf09`.
- Decision: free subdomain (no custom domain) → recurring cost lowered from ≤ $15/yr to **$0/yr**.
- Wrote [account-checklist.md](../account-checklist.md) for user to provision GCP / Grafana / Sentry / PostHog (Cloudflare already ready).
- Updated README and orchestrator-plan to reflect subdomain decision and account state.
- **Stream B complete (contracts freeze).** Commit `13c67b2`, tagged `contracts-v1.0.0`. Authored:
  - 4 proto files: `document.proto`, `events.proto`, `scorer.proto`, `embedder.proto`.
  - `gateway/src/schema.graphql` (GraphQL SDL with Query + Subscription).
  - `docs/api/openapi.yaml` (OpenAPI 3.1 REST surface).
  - `docs/api/websocket.md` (WS message catalog).
  - `docs/api/events.md` (NATS + Pub/Sub topic catalog).
  - `mcp-server/src/tools.ts` (8 MCP tool definitions).
  - `proto/gen/typescript/` skeleton for `@evidencelens/contracts` workspace package.
- Caught + fixed one pre-tag bug: `HealthzRequest`/`Response` collided between `scorer.proto` and `embedder.proto` (same package). Renamed to `ScorerHealthz` / `EmbedderHealthz`.

## Blocked on
- **GitHub push blocked**: `gh` CLI not installed, no SSH key for github.com, no credential helper. Local commits (`bfdcf09`, `13c67b2`) and tag (`contracts-v1.0.0`) accumulate until user installs `gh` per [account-checklist.md](../account-checklist.md) or creates the repo via web + pushes manually with HTTPS token.
- **buf lint not run locally** — neither `buf` nor Go is installed on this machine. Will validate on first CI run post-push. Manual review of the four protos was done.
- **GCP account** not yet provisioned — blocks Stream A Cloud Run deploy of ingesters (~Week 2). Local code unaffected.
- **Grafana Cloud / Sentry / PostHog** not yet provisioned — blocks Stream A observability wiring. Local code unaffected.

## Integration risk
- Push blocker is low-impact short-term (everything is local Git) but compounds: CI doesn't run, branch protection can't be configured, Actions can't run scheduled crons. Recommend resolving before more work piles up unverified.
- Contracts tag is "frozen" by convention but unvalidated by CI until first push. If CI catches a syntax error, fix-forward + retag is acceptable since nothing has consumed the contracts yet.

## ETA delta
- **Ahead of plan**: Stream B (contracts freeze) was Week 1 EOW deliverable; landed Day 1.
- **Stream A pushed to Week 2** pending account provisioning. Local Stream A scaffolding (terraform skeleton + docker-compose.nas.yml + .env.example) can begin in next session without accounts.
- **Stream C** can also begin locally next session (`ingest/pkg/ingestcommon/` is pure Go, no cloud needed for local dev). 12 ingester fan-out gated on having one template ingester complete.
