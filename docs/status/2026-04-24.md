# Status — 2026-04-24

**Stream lead (all):** Orchestrator (acting directly per user "rare parallelism" choice)

## Done since yesterday
- N/A — first day.

## Doing today
- Repo bootstrap: directory tree per spec §17, root files (README, CLAUDE.md, LICENSE, .gitignore, .editorconfig), workspace skeletons (pnpm-workspace.yaml, go.work, buf.yaml, buf.gen.yaml), Makefile, RFC process doc.
- Local `git init` + initial commit `bfdcf09`.
- Decision: free subdomain (no custom domain) → recurring cost lowered from ≤ $15/yr to **$0/yr**.
- Wrote [account-checklist.md](../account-checklist.md) for user to provision GCP / Grafana / Sentry / PostHog (Cloudflare already ready).
- Updated README and orchestrator-plan to reflect subdomain decision and account state.
- Started Stream B contract authoring (proto + GraphQL + OpenAPI + WebSocket + MCP catalog + events catalog).

## Blocked on
- **GitHub push blocked**: `gh` CLI not installed, no SSH key for github.com, no credential helper. User needs to either install `gh` and run the push command in [account-checklist.md](../account-checklist.md), or create the repo via web and push manually with HTTPS token. Local commits accumulate until then.
- **GCP account** not yet provisioned — blocks Stream A Cloud Run deploy of ingesters (~Week 2). Local code unaffected.
- **Grafana Cloud / Sentry / PostHog** not yet provisioned — blocks Stream A observability wiring. Local code unaffected.

## Integration risk
- Push blocker is low-impact short-term (everything is local Git) but compounds: CI doesn't run, branch protection can't be configured, Actions can't run scheduled crons.

## ETA delta
- On track for Week 1: Stream B (contracts freeze) achievable this session.
- Stream A pushed to Week 2 pending account provisioning.
